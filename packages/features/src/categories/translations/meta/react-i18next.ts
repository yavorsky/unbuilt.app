import { lazy } from 'react';
import { Meta } from '../../../types/meta.js';

export const reactI18next: Meta = {
  name: 'React-i18next',
  website: 'https://react.i18next.com/',
  tags: ['Translations'],
  description:
    "React-i18next is an internationalization-framework written in and for JavaScript. But it's much more than that.",
  Icon: lazy(() => import('./icons/react-i18next.jsx')),
};
